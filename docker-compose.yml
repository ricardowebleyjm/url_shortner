version: '3.7'

services:
  app:
    build: 
      context: .
    container_name: url_shortner
    restart: always
    volumes:
      - .:/code
    ports:
      - 9010:9010

    networks:
      - app-network

networks:
  app-network:
    driver: bridge
    external: true